apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: oms
spec:
  type: NodePort
  selector:
    app: order-service
  ports:
    - port: 50051
      targetPort: 50051
      nodePort: 30051
